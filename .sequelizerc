const path = require("path");

module.exports = {
  config: path.resolve("database", "config.js"), // change this to your config file
  "models-path": path.resolve("models"),
  "migrations-path": path.resolve("migrations"),
  "seeders-path": path.resolve("seeders")
};